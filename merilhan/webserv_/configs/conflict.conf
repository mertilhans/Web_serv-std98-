# Iki server, TAM AYNI (ip,port) + AYNI server_name (ikisi de "").
# Host header gelse bile hangisinin cevap verecegi belirsiz -> reddedilmeli.
server {
    listen 0.0.0.0:8080;
    root ./www;
}
server {
    listen 0.0.0.0:8080;
    root ./www2;
}
